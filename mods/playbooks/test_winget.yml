---
- name: Test module winget_cmd
  hosts: windows
  tasks:
    - name: Install firefox with custom module
      ansi_shared.mods.winget_cmd:
        id: Mozilla.Firefox
      register: output

    - name: Print output
      ansible.builtin.debug:
        var: output
